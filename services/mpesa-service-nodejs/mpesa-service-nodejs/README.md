# mpesa-service-nodejs
M-Pesa (Daraja) integration for Elastic FS (Express + Postgres). Supports STK Push, callback handling, and optional C2B URL registration + handlers.
See `src/routes/mpesa.js` for endpoints and `sql/schema.sql` for tables.
